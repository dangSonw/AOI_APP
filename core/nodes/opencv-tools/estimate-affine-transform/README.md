# Estimate affine transform node

Estimates a 2×3 affine transform from at least three corresponding `source-points` and `destination-points` using RANSAC or LMeDS. Status: `debug`; validate calibration points on target hardware.