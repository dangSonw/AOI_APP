# Switch / case node

Compares generic `value` against ordered JSON `cases`. Each case contains `branch` and `value`. First match routes its branch; no match routes `default`. Add matching custom control outputs in node inspector. Status: `debug`.